trigger:
- main

pool:
  name: Default

stages:
- stage: TerraformInit
  jobs:
  - job: Init
    steps:
    - script: terraform init
      displayName: Terraform Init

- stage: TerraformPlan
  jobs:
  - job: Plan
    steps:
    - script: terraform plan
      displayName: Terraform Plan